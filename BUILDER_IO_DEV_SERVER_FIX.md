# 🔧 Builder.io Dev Server Fix

## Проблема

**Ошибка:** `Dev server failed to start or connect exited with code 0`

## Причины

1. Команда `pnpm dev` может завершиться до того, как Builder.io успеет подключиться
2. Сервер может не открываться на `0.0.0.0`, только на `localhost`
3. Builder.io ожидает, что сервер будет работать постоянно

## Решение

### Вариант 1: Использовать флаг `--host 0.0.0.0` (Рекомендуется)

В Builder.io используйте следующую команду:

**Dev Command:**

```
pnpm dev --host 0.0.0.0
```

Это гарантирует, что сервер будет доступен на всех интерфейсах.

### Вариант 2: Обновить vite.config.ts (Уже сделано)

Конфигурация уже настроена с `host: true`, что эквивалентно `--host 0.0.0.0`.

### Вариант 3: Использовать npm вместо pnpm

Если pnpm не работает, попробуйте:

**Dev Command:**

```
npm run dev
```

**Package Manager:**

```
npm
```

### Вариант 4: Явно указать порт и хост

**Dev Command:**

```
pnpm dev --host 0.0.0.0 --port 3006
```

## Правильные настройки для Builder.io

### Environment Variables

```
VITE_PORT=3006
VITE_VERSION=2.5.8
VITE_BASE_URL=/
VITE_API_URL=/api/mock
VITE_BUILDER_API_KEY=your-builder-api-key-here
VITE_BUILDER_PREVIEW_URL=http://localhost:3006
NODE_ENV=development
```

### Setup Command

```
pnpm install
```

### Dev Command (Обновлено)

```
pnpm dev --host 0.0.0.0
```

ИЛИ

```
pnpm dev --host 0.0.0.0 --port 3006
```

### Package Manager

```
pnpm
```

## Альтернативные команды для тестирования

### Вариант A: С явным указанием хоста

```
pnpm dev --host 0.0.0.0 --port 3006
```

### Вариант B: Через npm (если pnpm не работает)

```
npm run dev
```

### Вариант C: С дополнительными флагами

```
pnpm vite --host 0.0.0.0 --port 3006
```

## Проверка работы

После настройки:

1. Убедитесь, что `.env` файл создан
2. Установите зависимости: `pnpm install`
3. Проверьте, что команда работает локально:
   ```bash
   pnpm dev --host 0.0.0.0
   ```
4. Сервер должен запуститься и показать:

   ```
   VITE v7.x.x  ready in xxx ms

   ➜  Local:   http://localhost:3006/
   ➜  Network: http://0.0.0.0:3006/
   ```

## Важные замечания

1. **Флаг `--host 0.0.0.0`** делает сервер доступным на всех сетевых интерфейсах
2. **Builder.io нужен именно `0.0.0.0`**, а не `localhost`
3. **Порт должен быть указан явно** через переменную окружения или флаг `--port`
4. **Команда должна работать постоянно** (не завершаться сразу)

## Итоговая рекомендация

**Используйте в Builder.io:**

**Dev Command:**

```
pnpm dev --host 0.0.0.0 --port 3006
```

Это гарантирует:

- ✅ Сервер доступен на всех интерфейсах
- ✅ Порт явно указан
- ✅ Совместимость с Builder.io
