# ✅ Builder.io - Финальная конфигурация

## 🎯 Правильные настройки для открытия сайта в Builder.io

### 📋 Обязательные настройки:

**Environment Variables:**

```
VITE_PORT=3006
VITE_VERSION=2.5.8
VITE_BASE_URL=/
VITE_API_URL=/api/mock
VITE_BUILDER_API_KEY=your-builder-api-key-here
VITE_BUILDER_PREVIEW_URL=http://localhost:3006
NODE_ENV=development
```

**Setup Command:**

```
pnpm install
```

**Dev Command (ВАЖНО!):**

```
pnpm dev --host 0.0.0.0 --port 3006
```

**Package Manager:**

```
pnpm
```

---

## ⚠️ Ключевые моменты для Builder.io

### 1. Dev Command должна быть:

```
pnpm dev --host 0.0.0.0 --port 3006
```

**НЕ используйте:**

- ❌ `pnpm dev` (без флагов)
- ❌ `npm run dev` (без флагов хоста)
- ❌ `vite` (без параметров)

### 2. Почему `--host 0.0.0.0`?

Builder.io требует, чтобы сервер был доступен на всех сетевых интерфейсах:

- ✅ `0.0.0.0` - доступен везде (нужно для Builder.io)
- ❌ `localhost` - только локально (не работает с Builder.io)

### 3. Preview URL Template

```
http://localhost:3006$PATHNAME
```

Или для продакшена:

```
https://your-domain.com$PATHNAME
```

---

## 🔧 Проверка конфигурации

### Что уже настроено в проекте:

✅ `vite.config.ts` - `host: true` (эквивалент `--host 0.0.0.0`) ✅ `vite.config.ts` - CORS заголовки настроены ✅ `vite.config.ts` - Порт 3006 настроен по умолчанию ✅ `.env` файл - можно создать с помощью `create-env.bat`

### Что нужно сделать в Builder.io:

1. ✅ Установить Environment Variables (см. выше)
2. ✅ Установить Setup Command: `pnpm install`
3. ✅ Установить Dev Command: `pnpm dev --host 0.0.0.0 --port 3006`
4. ✅ Установить Package Manager: `pnpm`
5. ✅ Установить Preview URL Template: `http://localhost:3006$PATHNAME`

---

## 🚀 Пошаговая настройка в Builder.io

### Шаг 1: Environment Variables

Вставьте все переменные из раздела выше.

### Шаг 2: Setup Command

```
pnpm install
```

### Шаг 3: Dev Command (ОБЯЗАТЕЛЬНО!)

```
pnpm dev --host 0.0.0.0 --port 3006
```

### Шаг 4: Package Manager

```
pnpm
```

### Шаг 5: Preview URL Template

```
http://localhost:3006$PATHNAME
```

### Шаг 6: Design Mode Selector

```
#app
```

---

## ✅ Проверка работы

После настройки Builder.io должен:

1. ✅ Успешно запустить dev сервер
2. ✅ Показать статус подключения
3. ✅ Открыть preview сайта
4. ✅ Позволить редактировать контент

---

## 🐛 Если сайт не открывается

### Проблема 1: "Dev server failed to start"

**Решение:**

- Проверьте, что Dev Command точно: `pnpm dev --host 0.0.0.0 --port 3006`
- Убедитесь, что все Environment Variables установлены
- Проверьте, что `pnpm install` выполнен успешно

### Проблема 2: "Cannot connect to server"

**Решение:**

- Убедитесь, что используется `--host 0.0.0.0`, а не `localhost`
- Проверьте, что порт 3006 свободен
- Проверьте CORS настройки в `vite.config.ts`

### Проблема 3: "Preview not loading"

**Решение:**

- Проверьте Preview URL Template: `http://localhost:3006$PATHNAME`
- Убедитесь, что Design Mode Selector: `#app`
- Проверьте, что сервер запущен и доступен

---

## 📝 Итоговая команда для копирования

**Просто скопируйте и вставьте в Builder.io:**

**Dev Command:**

```
pnpm dev --host 0.0.0.0 --port 3006
```

---

## ✅ Готово!

После применения этих настроек сайт должен нормально открываться в Builder.io!
