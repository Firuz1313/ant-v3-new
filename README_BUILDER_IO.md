# 🚀 Builder.io Setup - Краткая инструкция

## Быстрая настройка для Builder.io

### Шаг 1: Создайте файл `.env`

Создайте файл `.env` в корне проекта со следующим содержимым:

```env
VITE_PORT=3006
VITE_VERSION=2.5.8
VITE_BASE_URL=/
VITE_API_URL=/api/mock
VITE_BUILDER_API_KEY=your-builder-api-key-here
VITE_BUILDER_PREVIEW_URL=http://localhost:3006
NODE_ENV=development
```

**Замените `your-builder-api-key-here` на ваш реальный API ключ из Builder.io**

### Шаг 2: Настройки в Builder.io

При настройке проекта в Builder.io используйте:

#### Environment Variables:

```
VITE_PORT=3006
VITE_VERSION=2.5.8
VITE_BASE_URL=/
VITE_API_URL=/api/mock
VITE_BUILDER_API_KEY=your-builder-api-key-here
VITE_BUILDER_PREVIEW_URL=http://localhost:3006
NODE_ENV=development
```

#### Setup Command:

```bash
pnpm install
```

#### Dev Command:

```bash
pnpm dev
```

#### Package Manager:

```
pnpm
```

### Шаг 3: Проверка

После настройки:

1. Убедитесь, что `.env` файл создан
2. Установите зависимости: `pnpm install`
3. Запустите сервер: `pnpm dev`
4. Сервер должен запуститься на `http://localhost:3006`

### Решение проблем

**Ошибка: Dev server failed to start**

1. Убедитесь, что файл `.env` существует
2. Проверьте, что порт 3006 свободен
3. Установите зависимости: `pnpm install`
4. Запустите вручную: `pnpm dev`

**Ошибка: exited with code 0**

Это означает, что сервер завершился. Возможные причины:

- Отсутствует `.env` файл
- Неверный формат переменных окружения
- Порт уже занят другим процессом

**Решение:**

- Создайте `.env` файл с правильными переменными
- Проверьте логи в терминале
- Освободите порт 3006 или измените `VITE_PORT` в `.env`

### Дополнительная документация

- Подробная настройка: `BUILDER_IO_SETUP.md`
- Настройка переменных: `BUILDER_IO_ENV_SETUP.md`
- Конфигурация: `BUILDER_IO_CONFIG.md`
