# 🚀 Builder.io Dev Command - Правильная настройка

## ✅ Правильная команда для Dev Server

### Рекомендуемая команда:

```
pnpm dev --host 0.0.0.0 --port 3006
```

### Альтернативные варианты:

1. **С флагом хоста:**

   ```
   pnpm dev --host 0.0.0.0
   ```

2. **Через npm (если pnpm не работает):**

   ```
   npm run dev --host 0.0.0.0 --port 3006
   ```

3. **Напрямую через vite:**
   ```
   pnpm vite --host 0.0.0.0 --port 3006
   ```

## 📋 Полные настройки для Builder.io

### Environment Variables

```
VITE_PORT=3006
VITE_VERSION=2.5.8
VITE_BASE_URL=/
VITE_API_URL=/api/mock
VITE_BUILDER_API_KEY=your-builder-api-key-here
VITE_BUILDER_PREVIEW_URL=http://localhost:3006
NODE_ENV=development
```

### Setup Command

```
pnpm install
```

### Dev Command (Копируйте это!)

```
pnpm dev --host 0.0.0.0 --port 3006
```

### Package Manager

```
pnpm
```

## ⚠️ Почему это важно

1. **`--host 0.0.0.0`** - делает сервер доступным на всех сетевых интерфейсах (необходимо для Builder.io)
2. **`--port 3006`** - явно указывает порт (избегает конфликтов)
3. **Builder.io требует `0.0.0.0`**, а не `localhost`

## ✅ Проверка

После вставки команды в Builder.io, сервер должен:

- Запуститься и работать постоянно
- Быть доступен на `http://0.0.0.0:3006`
- Показать статус подключения в Builder.io

## 🔍 Отладка

Если команда все еще не работает:

1. Проверьте, что все зависимости установлены:

   ```bash
   pnpm install
   ```

2. Проверьте, что переменные окружения установлены в Builder.io

3. Попробуйте альтернативную команду:
   ```
   npm run dev --host 0.0.0.0 --port 3006
   ```
