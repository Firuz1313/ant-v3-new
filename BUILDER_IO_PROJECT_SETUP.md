# 📋 Builder.io Project Setup - Рекомендуемые настройки

## Информация о настройках и рекомендуемые значения

### ✅ Обязательные настройки

#### 1. **Project Name** (Название проекта)

```
ant-v3-new
```

**Информация:** Название вашего проекта в Builder.io. Используется для идентификации проекта.

---

#### 2. **Framework Preset** (Пресет фреймворка)

```
Vue 3
```

**Информация:** Выберите Vue 3, так как проект использует Vue 3.5.21 с Composition API.

**Альтернативы:**

- Vue 3 (рекомендуется)
- Custom (если нужна кастомная настройка)

---

#### 3. **Main Branch Name** (Название основной ветки)

```
main
```

**Информация:** Название основной ветки Git. В вашем проекте используется `main`.

**Дополнительно:** Builder.io будет отслеживать изменения в этой ветке для автоматических деплоев.

---

### ⚙️ Рекомендуемые настройки

#### 4. **Memory Limit** (Лимит памяти)

```
4096 MB (4 GB)
```

**Информация:** Лимит памяти для выполнения сборки. Для Vue 3 проекта рекомендуется минимум 2GB, оптимально 4GB.

**Рекомендации:**

- Минимум: `2048 MB` (2 GB)
- Оптимально: `4096 MB` (4 GB)
- Для больших проектов: `8192 MB` (8 GB)

---

#### 5. **Fusion Execution Environment** (Среда выполнения)

```
Use space settings
```

**Информация:** Оставьте "Use space settings" для наследования настроек организации, или выберите:

- Standard (для большинства проектов)
- Large (для больших проектов с большими зависимостями)

---

#### 6. **Commit Mode** (Режим коммитов)

```
Automatic
```

**Информация:** Режим создания коммитов при деплое:

- **Automatic** - Builder.io автоматически создает коммиты
- **Manual** - Коммиты создаются вручную

**Рекомендация:** Используйте `Automatic` для упрощения процесса.

---

#### 7. **Default Branch Type** (Тип ветки по умолчанию)

```
Feature Branch
```

**Информация:** Тип ветки для новых фич:

- **Feature Branch** - отдельная ветка для каждой фичи
- **Main Branch** - коммиты напрямую в main

**Рекомендация:** `Feature Branch` для безопасности и изоляции изменений.

---

#### 8. **Node.js Version** (Версия Node.js)

```
20.x
```

**Информация:** Версия Node.js для выполнения проекта. В `package.json` указано: `"node": ">=20.19.0"`.

**Рекомендуемые значения:**

- `20.x` (рекомендуется, соответствует package.json)
- `20.19.0` (точная версия, если доступна)

---

#### 9. **Docker Image Path** (Путь к Docker образу)

```
registry-1.docker.io/library/node:20
```

**Информация:** Кастомный Docker образ для выполнения. Требует Enterprise подписку.

**Рекомендация:** Оставьте пустым для использования стандартного образа, или используйте:

- `registry-1.docker.io/library/node:20` (Node.js 20)
- `registry-1.docker.io/library/node:20-alpine` (легковесная версия)

---

#### 10. **Require Desktop App** (Требовать Desktop App)

```
Disabled (false)
```

**Информация:** Требовать ли использование Builder.io Desktop App.

**Рекомендация:** `Disabled` для работы через веб-интерфейс.

---

### 🎯 Опциональные настройки

#### 11. **Error Ignore Patterns** (Паттерны игнорирования ошибок)

```
^ResizeObserver loop limit exceeded$
^Non-Error promise rejection captured$
^ChunkLoadError.*
```

**Информация:** Регулярные выражения для игнорирования клиентских ошибок. Эти ошибки не будут показывать попапы.

**Рекомендуемые паттерны:**

- `^ResizeObserver loop limit exceeded$` - игнорирует ResizeObserver ошибки
- `^Non-Error promise rejection captured$` - игнорирует некритичные отклонения промисов
- `^ChunkLoadError.*` - игнорирует ошибки загрузки чанков (при обновлении)

---

#### 12. **Include Patterns** (Паттерны включения файлов)

```
src/**/*
public/**/*
*.json
*.md
vite.config.ts
tsconfig.json
```

**Информация:** Указывает, какие части репозитория клонировать.

**Рекомендации для Vue 3 проекта:**

- `src/**/*` - все исходные файлы
- `public/**/*` - публичные файлы
- `*.json` - все JSON файлы (package.json, tsconfig.json и т.д.)
- `*.md` - все markdown файлы (документация)
- `vite.config.ts` - конфигурация Vite
- `tsconfig.json` - конфигурация TypeScript
- `.env.example` - пример файла окружения (если есть)

**Оставить пустым** для клонирования всего репозитория.

---

#### 13. **Preview URL Template** (Шаблон URL превью)

```
http://localhost:3006$PATHNAME
```

**Информация:** Шаблон URL для предпросмотра в Builder.io редакторе.

**Рекомендуемое значение для разработки:**

```
http://localhost:3006$PATHNAME
```

**Для продакшена:**

```
https://your-domain.com$PATHNAME
```

**Доступные переменные:**

- `$URL` - полный URL
- `$PROTOCOL` - протокол (http/https)
- `$ORIGIN` - origin (http://localhost:3006)
- `$HOST` - хост (localhost)
- `$HOSTNAME` - имя хоста (localhost)
- `$PORT` - порт (3006)
- `$PATHNAME` - путь (/app/route)
- `$SEARCH` - query string (?param=value)
- `$HASH` - hash (#section)

**Примеры:**

- Локальная разработка: `http://localhost:3006$PATHNAME`
- Продакшен: `https://example.com$PATHNAME`
- С хешем: `http://localhost:3006$PATHNAME$HASH`

---

#### 14. **Design Mode Selector** (Селектор режима дизайна)

```
#app
```

**Информация:** CSS селектор для целевого элемента или iframe для парсинга режима дизайна.

**Рекомендуемые значения:**

- `#app` - основной контейнер приложения
- `#app-main` - главный контент (из вашего проекта)
- `.app-content` - контент приложения
- Оставить пустым для поведения по умолчанию

**Примеры:**

- `iframe[src*="iframe.html"]` - для iframe
- `#preview-container` - для контейнера превью
- `.main-content` - для основного контента

---

#### 15. **Git Branch Naming** (Именование веток Git)

```
feature/{{name}}
```

**Информация:** Шаблон именования веток Git при создании новых фич.

**Рекомендуемые шаблоны:**

- `feature/{{name}}` - feature/branch-name
- `fix/{{name}}` - fix/branch-name
- `feat/{{name}}` - feat/branch-name

**Доступные переменные:**

- `{{name}}` - название ветки (генерируется автоматически)
- `{{timestamp}}` - временная метка

---

## 📝 Итоговый чеклист настроек

### ✅ Обязательные поля:

1. **Project Name:** `ant-v3-new`
2. **Framework Preset:** `Vue 3`
3. **Main Branch Name:** `main`

### ⚙️ Рекомендуемые значения:

4. **Memory Limit:** `4096 MB` (4 GB)
5. **Fusion Execution Environment:** `Use space settings`
6. **Commit Mode:** `Automatic`
7. **Default Branch Type:** `Feature Branch`
8. **Node.js Version:** `20.x`
9. **Docker Image Path:** _(оставить пустым)_
10. **Require Desktop App:** `Disabled`

### 🎯 Опциональные настройки:

11. **Error Ignore Patterns:**

```
^ResizeObserver loop limit exceeded$
^Non-Error promise rejection captured$
^ChunkLoadError.*
```

12. **Include Patterns:**

```
src/**/*
public/**/*
*.json
*.md
vite.config.ts
tsconfig.json
```

13. **Preview URL Template:**

```
http://localhost:3006$PATHNAME
```

14. **Design Mode Selector:**

```
#app
```

15. **Git Branch Naming:**

```
feature/{{name}}
```

---

## 🔍 Дополнительная информация о проекте

### Технологический стек:

- **Framework:** Vue 3.5.21
- **Language:** TypeScript 5.6.3
- **Build Tool:** Vite 7.1.5
- **UI Framework:** Element Plus 2.11.2
- **Package Manager:** pnpm 10.18.3
- **Node.js:** >= 20.19.0

### Структура проекта:

- `src/` - исходные файлы
- `public/` - публичные файлы
- `vite.config.ts` - конфигурация Vite
- `tsconfig.json` - конфигурация TypeScript
- `package.json` - зависимости и скрипты

### Порт и URL:

- **Dev Port:** 3006
- **Base URL:** `/`
- **Entry Point:** `/#/auth/login`

### Builder.io интеграция:

- **SDK:** @builder.io/sdk-vue@4.2.4
- **Component:** `src/components/builder/BuilderComponent.vue`

---

## ✅ Готово!

Все настройки готовы для копирования и вставки в Builder.io!
