# ✅ Builder.io Project Settings - Готовые настройки

## 🎯 Все настройки готовы к копированию

### Основные настройки

| Поле                             | Значение             | Информация                             |
| -------------------------------- | -------------------- | -------------------------------------- |
| **Project Name**                 | `ant-v3-new`         | Название проекта                       |
| **Framework Preset**             | `Vue 3`              | Фреймворк: Vue 3.5.21                  |
| **Main Branch Name**             | `main`               | Основная ветка Git                     |
| **Memory Limit**                 | `4096 MB`            | 4 GB для комфортной сборки             |
| **Fusion Execution Environment** | `Use space settings` | Наследовать от организации             |
| **Commit Mode**                  | `Automatic`          | Автоматические коммиты                 |
| **Default Branch Type**          | `Feature Branch`     | Отдельная ветка для фич                |
| **Node.js Version**              | `20.x`               | Соответствует package.json (>=20.19.0) |
| **Docker Image Path**            | _(оставить пустым)_  | Использовать стандартный образ         |
| **Require Desktop App**          | `Disabled`           | Работа через веб-интерфейс             |

---

### Error Ignore Patterns

Скопируйте и вставьте (по одному паттерну на строку):

```
^ResizeObserver loop limit exceeded$
^Non-Error promise rejection captured$
^ChunkLoadError.*
```

**Информация:** Эти паттерны игнорируют некритичные ошибки в браузере:

- `ResizeObserver` - распространенная ошибка от наблюдателей размеров
- `Non-Error promise rejection` - некритичные отклонения промисов
- `ChunkLoadError` - ошибки загрузки при обновлении кода

---

### Include Patterns

Скопируйте и вставьте (по одному паттерну на строку):

```
src/**/*
public/**/*
*.json
*.md
vite.config.ts
tsconfig.json
.env.example
```

**Информация:** Указывает, какие файлы включать при клонировании:

- `src/**/*` - все исходные файлы проекта
- `public/**/*` - публичные файлы (изображения, favicon)
- `*.json` - конфигурационные файлы (package.json, tsconfig.json)
- `*.md` - документация
- `vite.config.ts` - конфигурация сборщика
- `tsconfig.json` - конфигурация TypeScript
- `.env.example` - пример переменных окружения

**Альтернатива:** Оставить пустым для клонирования всего репозитория.

---

### Preview URL Template

```
http://localhost:3006$PATHNAME
```

**Информация:** Шаблон URL для предпросмотра в Builder.io редакторе.

**Доступные переменные:**

- `$PATHNAME` - путь маршрута (например: `/dashboard/analytics`)
- `$HOSTNAME` - хост (localhost)
- `$PORT` - порт (3006)
- `$URL` - полный URL
- `$ORIGIN` - origin (http://localhost:3006)

**Для продакшена замените на:**

```
https://your-domain.com$PATHNAME
```

---

### Design Mode Selector

```
#app
```

**Информация:** CSS селектор для целевого элемента приложения. В вашем проекте Vue приложение монтируется в `#app` (см. `index.html` и `main.ts`).

**Альтернативы (если нужны):**

- `#app-main` - главный контейнер контента
- `.app-content` - контейнер контента
- Оставить пустым для поведения по умолчанию

---

### Git Branch Naming

```
feature/{{name}}
```

**Информация:** Шаблон именования веток Git для новых фич.

**Примеры:**

- `feature/builder-integration` - для интеграции Builder.io
- `feature/user-dashboard` - для дашборда пользователя
- `fix/login-error` - для исправления ошибки (если используете префикс `fix/`)

**Доступные переменные:**

- `{{name}}` - название ветки (генерируется автоматически)
- `{{timestamp}}` - временная метка

---

## 📋 Чеклист заполнения в Builder.io

### ✅ Шаг 1: Основные настройки

- [ ] Project Name: `ant-v3-new`
- [ ] Framework Preset: `Vue 3`
- [ ] Main Branch Name: `main`
- [ ] Memory Limit: `4096 MB`
- [ ] Fusion Execution Environment: `Use space settings`
- [ ] Commit Mode: `Automatic`
- [ ] Default Branch Type: `Feature Branch`
- [ ] Node.js Version: `20.x`
- [ ] Docker Image Path: _(оставить пустым)_
- [ ] Require Desktop App: `Disabled`

### ✅ Шаг 2: Опциональные настройки

- [ ] Error Ignore Patterns: _(скопировать паттерны выше)_
- [ ] Include Patterns: _(скопировать паттерны выше)_
- [ ] Preview URL Template: `http://localhost:3006$PATHNAME`
- [ ] Design Mode Selector: `#app`
- [ ] Git Branch Naming: `feature/{{name}}`

---

## 🚀 После заполнения настроек

1. Сохраните настройки в Builder.io
2. Убедитесь, что все переменные окружения установлены (см. `BUILDER_IO_FINAL_SETUP.md`)
3. Проверьте, что dev сервер запущен на `http://localhost:3006`
4. Протестируйте Preview URL в Builder.io редакторе

---

## ✅ Готово!

Все настройки готовы к использованию. Просто скопируйте значения из этого документа и вставьте в Builder.io!
